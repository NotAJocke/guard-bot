import Config from "../models/config";

const config: Config = {
	intents: [],
	loggingChannels: {
		ban: {
			name: "",
			id: "",
		},
	},
	communityRoleVerified: "",
	guildId: "",
};

export default config;
